# ПРОСТО — Документ стилей (DESIGN.md)

Гайдлайн визуального языка молодёжного пространства **«ПРОСТО»** (`prostospb.team`) — сеть бесплатных коворкингов, мероприятий и просветительских проектов в Санкт-Петербурге для людей 14–35 лет.

> ⚠️ **О точности значений.** Цвета и параметры в этом документе сняты визуально со скриншотов интерфейса — исходный CSS прочитать не удалось. Поэтому hex-коды, размеры и названия шрифтов **близкие, но не побайтовые**. Перед использованием как «источника истины» сверьте ключевые токены через DevTools браузера (вкладка *Computed*) или экспорт стилей. Места, где точность особенно важна, помечены значком 🔍.

---

## Содержание

1. [Характер бренда](#1-характер-бренда)
2. [Цвет](#2-цвет)
3. [Типографика](#3-типографика)
4. [Сетка и отступы](#4-сетка-и-отступы)
5. [Скругления, обводки, тени](#5-скругления-обводки-тени)
6. [Кнопки](#6-кнопки)
7. [Навигация](#7-навигация)
8. [Карточки](#8-карточки)
9. [Бейджи и плашки](#9-бейджи-и-плашки)
10. [Формы и поля ввода](#10-формы-и-поля-ввода)
11. [Модальные окна](#11-модальные-окна)
12. [Выбор даты и времени](#12-выбор-даты-и-времени)
13. [Футер](#13-футер)
14. [Аватары](#14-аватары)
15. [Иконки и графика](#15-иконки-и-графика)
16. [Фотография](#16-фотография)
17. [Анимация](#17-анимация)
18. [Доступность](#18-доступность)
19. [Тон голоса](#19-тон-голоса)
20. [Сводка CSS-токенов](#20-сводка-css-токенов)

---

## 1. Характер бренда

ПРОСТО — государственное учреждение (СПб ГБУ), но визуально это подчёркнуто **молодёжный, тёплый и неформальный** бренд. Государственная «серьёзность» почти не считывается; вместо неё — энергия, простота и человечность.

**Ключевые принципы языка:**

- **Человек в центре.** Главный слоган первого экрана — `это люди <3`. Фотографии — про живых людей, объятия, общение, а не про мебель.
- **Смело и крупно.** Огромная жирная типографика, контрастные акценты, минимум «воды».
- **Дружелюбно и на «ты».** Тексты разговорные: «Выбери точку», «Прокачай свой проект», даже куки-баннер звучит как «Мы собираем ваши cookies, чтобы кормить наших админов».
- **Тёмная база, яркие искры.** Чёрный фон создаёт сцену, на которой ярко работают фиолетовый, кислотно-зелёный и живая фотография.
- **Скруглённость и «плавучесть».** Навбар, карточки, кнопки, секции — всё с крупными радиусами и ощущением, будто блоки «парят» над фоном.

Тон: *уверенный, тёплый, ироничный, доступный.* Никакого канцелярита в интерфейсе (он остаётся только в обязательных юридических ссылках футера).

---

## 2. Цвет

Палитра построена на **чёрно-белом каркасе** с двумя яркими акцентами — фиолетовым и кислотно-зелёным (лаймом). Цвет используется дозированно: основная масса экрана нейтральная, акценты — точечно (бейджи, графика, иллюстрации).

### Основа (нейтральные) 🔍

| Токен | Hex (≈) | Где используется |
|---|---|---|
| `--bg` | `#000000` | Основной фон страницы, футер, первый экран |
| `--surface` | `#151515` | Тёмные карточки новостей, контейнеры |
| `--surface-elevated` | `#1F1F1F` | Поля ввода, карточки дат, модальные окна |
| `--stroke` | `rgba(255,255,255,.08)` | Тонкие обводки карточек и модалок (≈ `#262626`) |
| `--text-primary` | `#FFFFFF` | Заголовки, основные подписи, текст на тёмном |
| `--text-secondary` | `#A2A2A2` | Описания в карточках, вторичный текст |
| `--text-muted` | `#6B6B6B` | Юридический мелкий текст, копирайт |

### Акценты 🔍

| Токен | Hex (≈) | Где используется |
|---|---|---|
| `--violet` | `#8C52FF` | Главный акцент: бейджи, фон карточек, иллюстрации, заголовки на светлом |
| `--violet-deep` | `#5B2BD6` | Тёмный вариант фиолетового (плашка «Приём заявок…» на фиолетовом фоне) |
| `--lime` | `#C8FF1E` | Второй акцент: молнии, неоновые «загогулины», графические штрихи поверх фото |

### Светлые поверхности 🔍

| Токен | Hex (≈) | Где используется |
|---|---|---|
| `--white-surface` | `#FFFFFF` | Светлые карточки и секции (напр. блок «Решаем вместе») |
| `--cream` | `#F1EFEA` | Тёплый светлый фон карточек («ПРОСТО о грантах») |
| `--ink-on-light` | `#141414` | Текст и заголовки на светлых поверхностях |

### Правила использования цвета

- **Дефолт — тёмный.** Все основные секции на `--bg` с белым текстом. Светлые карточки/секции — акцент, а не правило.
- **Один акцент на блок.** Фиолетовый и лайм почти не смешиваются в равных долях; лайм чаще работает как тонкий графический штрих поверх фото/фиолетового.
- **Текст вторичного уровня — серый, не приглушённый белый.** Описания карточек заметно темнее заголовков (`--text-secondary`).
- **Контраст заголовков максимальный** — чисто белый на чёрном.

> **Внешние виджеты.** Блок «Решаем вместе» (Госуслуги / ПОС) использует свою фирменную палитру — синий (≈ `#0D4CD3`) и бежево-коралловую иллюстрацию. Это сторонний embed: **не считать частью токенов ПРОСТО** и не тиражировать в собственных компонентах.

---

## 3. Типографика

Типографика — главный носитель характера. Два пласта: **тяжёлый дисплейный** для заголовков и **нейтральный гротеск** для текста.

### Гарнитуры 🔍

- **Дисплейная (заголовки, логотип).** Очень жирный геометрический гротеск с округлой геометрией: идеально круглая «О», плоские терминалы, массивные штрихи. Используется в самом тяжёлом начертании (Black / 800–900), преимущественно **ПРОПИСНЫМИ**. Визуально близко к гарнитурам семейства **Unbounded / Involve / TT Travels Next** или кастомному начертанию.
  *Рекомендуемый стек (до подтверждения точной гарнитуры):*
  ```css
  font-family: "Unbounded", "Involve", "TT Norms Pro", system-ui, sans-serif;
  ```
- **Текстовая (описания, поля, навигация).** Чистый нейтральный гротеск с лёгкой гуманистичностью, начертания Regular/Medium. Визуально близко к **Inter / Manrope / Onest**.
  ```css
  font-family: "Onest", "Inter", "Manrope", system-ui, sans-serif;
  ```
- **Курсив.** Жирный курсив дисплейной гарнитуры используется точечно для эмоциональных подписей (подзаголовок первого экрана `это люди <3`).

### Шкала размеров (десктоп, ≈) 🔍

| Роль | Размер | Начертание | Регистр | Пример |
|---|---|---|---|---|
| Hero display | `clamp(72px, 14vw, 180px)` | Black 900 | UPPERCASE | «ПРОСТО» |
| Заголовок секции | `clamp(48px, 6vw, 80px)` | Black 800 | как есть | «Новости», «Выбери точку» |
| Заголовок модалки | `40–48px` | Bold 800 | как есть | «Вход» |
| Заголовок карточки | `28–32px` | Bold 700 | как есть | «Прожарка проектов» |
| Hero-подзаголовок | `28–36px` | Bold Italic | lowercase | «это люди <3» |
| Меню / навигация | `17–18px` | Medium 500 | как есть | «Коворкинги» |
| Кнопка | `17–18px` | Medium 600 | как есть | «Забронировать помещение» |
| Body / описание | `16–18px` | Regular 400 | как есть | текст карточек |
| Лейбл поля / бейдж | `13–14px` | Medium 500 | как есть | «Эл.почта», «новый маршрут» |

### Правила

- **Заголовки — максимально крупно и жирно.** Дисплейная гарнитура почти всегда в самом тяжёлом весе.
- **Никакого разрядки в верхнем регистре** — буквы стоят плотно.
- **Контраст веса:** очень жирные заголовки ↔ обычный (Regular) текст. Полужирный в body почти не нужен.
- **Межстрочный интервал** в заголовках плотный (≈ `1.0–1.05`), в body — свободный (`1.4–1.5`).

---

## 4. Сетка и отступы

- **Контейнер:** центрированный, максимальная ширина ≈ `1280–1440px`, боковые поля ≈ `24–40px` (на десктопе шире).
- **Сетка карточек:** 3 колонки на десктопе, крупные промежутки (gap ≈ `24–32px`); на планшете — 2, на мобильном — 1.
- **Вертикальный ритм:** между крупными секциями большие отступы (≈ `96–128px`). Воздуха много.
- **«Плавающая» компоновка:** навбар и многие секции имеют боковые отступы от краёв экрана, а не приклеены к ним — отсюда ощущение карточек, лежащих на чёрном.

### Шкала отступов (4px-база)

```
4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96 · 128
```

```css
--space-1: 4px;   --space-2: 8px;   --space-3: 12px;  --space-4: 16px;
--space-5: 24px;  --space-6: 32px;  --space-7: 48px;  --space-8: 64px;
--space-9: 96px;  --space-10: 128px;
```

---

## 5. Скругления, обводки, тени

**Крупные радиусы — узнаваемая черта бренда.** Скруглено почти всё.

| Токен | Значение (≈) | Где |
|---|---|---|
| `--radius-pill` | `999px` | Кнопки, поля ввода, бейджи |
| `--radius-sm` | `16px` | Карточки дат, мелкие элементы |
| `--radius-card` | `24px` | Карточки новостей/проектов |
| `--radius-nav` | `28px` | Плавающий навбар |
| `--radius-section` | `32–40px` | Крупные светлые секции |

**Обводки.** На тёмной теме — тонкая полупрозрачная белая линия (`--stroke`) у карточек, модалок и полей; даёт мягкое разделение без жёстких границ.

**Тени.** Минимальны — тема плоская. Глубина создаётся не тенями, а контрастом поверхностей (`--bg` → `--surface` → `--surface-elevated`). У модального окна — затемнение фона:

```css
--backdrop: rgba(0, 0, 0, .6);
```

---

## 6. Кнопки

### Primary (светлая) — основное действие

Главный тип кнопок (CTA первого экрана).

- Форма: **pill** (`--radius-pill`), полностью скруглённая.
- Фон: `#FFFFFF`, текст: `--ink-on-light` (тёмный), Medium 600.
- Крупная, с щедрыми внутренними отступами (≈ `18–22px` по вертикали, `32px+` по горизонтали).
- Hover (предположительно): лёгкое затемнение фона / масштаб.

```css
.btn-primary {
  background: #fff; color: #141414;
  border-radius: var(--radius-pill);
  padding: 20px 36px; font-weight: 600; font-size: 18px;
  border: none; cursor: pointer;
}
```

### Primary (тёмная) / disabled

Тёмная pill-кнопка на тёмном фоне (напр. «Войти» в модалке выглядит как неактивное/тёмное состояние).

- Фон: `--surface-elevated`, текст приглушённо-белый.
- Используется для второстепенного или неактивного действия.

### Icon-кнопки

- **Круглые, светлые** (в навбаре): белый круг, тёмная иконка внутри (регистрация/QR и аккаунт).
- **Круглая тёмная** (кнопка «наверх» в футере): тёмный круг с белым шевроном `↑`.

### Outline (сторонний контекст)

Кнопка «Написать» в блоке Госуслуг — белый фон, синяя обводка, синий текст, умеренное скругление. Это стиль внешнего виджета; **не использовать** как нативную кнопку ПРОСТО.

---

## 7. Навигация

**Плавающий тёмный навбар** — фирменный элемент.

- Контейнер: почти чёрный (`--surface`/`--bg`), `--radius-nav`, с боковыми отступами от краёв экрана; «парит» над контентом, прилипает при скролле (sticky).
- **Слева:** логотип `ПРОСТО` — белый, тяжёлый дисплейный шрифт.
- **По центру:** пункты меню — `Коворкинги · Мероприятия · Спецпроекты · Об организации`, белые, Medium, с крупными промежутками.
- **Справа:** две круглые иконки-кнопки — *регистрация* (иконка-QR/бейдж) и *аккаунт* (иконка пользователя), белые круги с тёмной иконкой.
- На светлых секциях навбар сохраняет тёмный вид, оставаясь поверх (видно на стыке со светлым блоком).

---

## 8. Карточки

Карточки — основной строительный блок ленты («Новости»). Два подвида.

### A. Тёмная медиа-карточка

- Контейнер: `--surface`, `--radius-card`, тонкая обводка `--stroke`.
- **Верх:** медиа на всю ширину карточки — *фотография* (интерьер, люди) или *цветная иллюстрация*. Углы медиа скруглены заодно с карточкой.
- **Низ:** заголовок (`--text-primary`, Bold) + описание (`--text-secondary`, Regular). Внутренние отступы ≈ `24–28px`.

### B. Цветная иллюстративная карточка

- Фон целиком фирменного цвета или иллюстрация: фиолетовый, кремовый, тёмно-бордовое фото и т.п.
- Текст — контрастный к фону (тёмный на светлом, белый на тёмном).
- Часто содержит крупную графику бренда (3D-персонажи, молнии, неоновые штрихи).

**Общее:** одинаковый радиус, одинаковые отступы, единая сетка; чередование тёмных и цветных карточек создаёт ритм ленты.

---

## 9. Бейджи и плашки

Небольшие маркеры поверх карточек.

- Форма: **pill** (`--radius-pill`), малый размер, текст `13–14px` Medium.
- Варианты:
  - **Фиолетовый бейдж** (`--violet` / `--violet-deep`) с белым текстом — статус/дедлайн: «Приём заявок до 13 июня».
  - **Бейдж-заголовок проекта** на фото: фиолетовая плашка «ПРОВОДНИКИ СМЫСЛОВ» + подпись «новый маршрут».
- Размещение: левый верхний угол медиа карточки, с отступом от края.

---

## 10. Формы и поля ввода

(Видно на экране входа и регистрации.)

- **Лейбл** над полем: `--text-secondary`, мелкий (`13–14px`).
- **Поле ввода:** фон `--surface-elevated`, крупное скругление (pill или `~16px`), внутренний отступ ≈ `18–20px`, плейсхолдер серый (`ivanovanov@mail.ru`).
- **Поле пароля:** иконка-«глаз» справа для показа/скрытия.
- **Вспомогательная ссылка** под полем: белый текст, подчёркивание («Не помню пароль»).
- **Кнопка submit:** на всю ширину формы, pill; в неактивном состоянии — тёмная/приглушённая.
- **Ссылка-переход** под кнопкой: белый bold-текст («Нет аккаунта? Зарегистрируйся»).

```css
.field {
  background: var(--surface-elevated); color: #fff;
  border-radius: var(--radius-pill);
  padding: 18px 22px; border: 1px solid var(--stroke);
}
.field::placeholder { color: var(--text-muted); }
```

---

## 11. Модальные окна

- Контейнер: тёмная карточка (`--surface`/`--surface-elevated`), `--radius-card`, тонкая обводка `--stroke`, центрирован.
- Фон страницы затемняется (`--backdrop`) и слегка размывается; контент за модалкой «приглушён».
- **Структура (на примере «Вход»):**
  1. Круглый **3D-аватар** (мемоджи) сверху.
  2. Крупный заголовок (Bold).
  3. Поля формы.
  4. Вспомогательная ссылка.
  5. Primary-кнопка на всю ширину.
  6. Текст-переход внизу.
- Щедрые внутренние отступы, центрированные элементы.

---

## 12. Выбор даты и времени

(Экран записи в коворкинг.)

- **Карточки дней:** скруглённые «плитки» (`--radius-sm`), тёмные (`--surface-elevated`), с днём недели сверху (`Пт`, `Сб`, `Вс`) и числом крупно под ним.
- **Выбранное состояние:** плитка светлее/выделена (≈ светло-серый фон).
- **Переключатели вида** (календарь / сетка) в правом верхнем углу — иконки-кнопки, активная подсвечена белым.
- Подзаголовки шагов крупные: «Выбери точку», «Выбери время».

---

## 13. Футер

- Фон: чистый `--bg` (чёрный).
- **Логотип** `ПРОСТО®` — белый, с маленьким знаком ®.
- **Колонки ссылок:**
  - навигация: «О проекте», «Команда»;
  - соцсети: «ВКонтакте», «Telegram», «MAX».
- **Юридический блок** (мельче, `--text-muted`/`--text-secondary`): «Стандарт обеспечения пространств», «Политика конфиденциальности», «Правила молодёжных пространств ПРОСТО», «Противодействие коррупции», «Сообщить о проблеме», «Противодействие идеологии терроризма».
- **Копирайт:** «(с) Санкт-Петербургское государственное бюджетное учреждение „Молодёжные пространства „ПРОСТО"» + уведомление об использовании материалов — серым, мелким.
- **Кнопка «наверх»:** круглая тёмная, нижний правый угол, белый шеврон `↑`.
- Много вертикального воздуха между группами ссылок.

---

## 14. Аватары

- Стиль: **3D «мемоджи»** — дружелюбные объёмные персонажи (машущий человек в белой шапочке; человек за ноутбуком).
- Форма: круг.
- Использование: профиль/вход, персонализация. Поддерживают тёплый, человечный тон бренда.

---

## 15. Иконки и графика

- **UI-иконки:** простые, читаемые — пользователь, регистрация/QR, календарь, сетка, «глаз», шеврон, стрелки. Монохром (белый на тёмном / тёмный на светлом).
- **Декоративная графика бренда:**
  - неоновые **«загогулины»** и **молнии** лаймового цвета поверх фото и иллюстраций;
  - **3D-персонажи** и предметные иллюстрации (кроссовки на стопке книг, фигурки) — в фирменных цветах;
  - графика подаётся смело, крупно, поверх фото или цветных подложек.
- Иконки-кнопки — всегда в круге (см. §6).

---

## 16. Фотография

- **Сюжет — люди и эмоции:** объятия, общение, совместная работа; молодые, разные люди.
- **Свет — тёплый и атмосферный:** приглушённое освещение, акцентные пятна (характерное пурпурно-розовое подсвечивание интерьеров, кольцевые лампы, растения).
- **Город как контекст:** узнаваемый Петербург (наб. каналов, классическая архитектура) в кадрах со спецпроектами.
- **Обработка:** живая, не «стоковая»; допускается тёмная тонировка, чтобы фото ложилось в общую тёмную тему.
- Фото часто комбинируется с графическими штрихами бренда (лаймовые линии, бейджи).

---

## 17. Анимация

*Раздел частично предположительный — снят по поведению, не по коду.* 🔍

- **Видео-фон первого экрана:** зацикленный ролик за гигантским заголовком «ПРОСТО».
- Ожидаемые микровзаимодействия: плавные hover-состояния кнопок/карточек, мягкое появление секций при скролле, sticky-навбар.
- Принцип: анимация поддерживает «живость», но не перетягивает внимание; переходы плавные, без резкости.

---

## 18. Доступность

- **Контраст.** Белый на чёрном — отличный контраст для заголовков. Проверьте `--text-secondary` (`#A2A2A2`) и `--text-muted` (`#6B6B6B`) на тёмном: серый текиле может не дотягивать до WCAG AA для мелких размеров — для основного body держите текст светлее `#A2A2A2` или крупнее `16px`.
- **Текст на фото.** Поверх фотографий (как hero) используйте затемняющий слой/градиент, чтобы белый текст оставался читаемым.
- **Состояния.** У ссылок «Не помню пароль» уже есть подчёркивание — хорошо для распознаваемости; сохраняйте видимые focus-стили для клавиатурной навигации.
- **Размер целей.** Pill-кнопки и иконки-кнопки крупные — это хорошо для тач-устройств (цель ≥ 44×44px).

---

## 19. Тон голоса

Голос ПРОСТО — **тёплый, на «ты», с лёгкой иронией.**

- Обращение на «ты», глаголы-приглашения: «Выбери точку», «Выбери время», «Прокачай свой проект».
- Эмоциональные акценты вместо канцелярита: `это люди <3`, эмодзи-сердечки.
- Самоирония: куки-баннер — «Мы собираем ваши cookies, чтобы кормить наших админов».
- Коротко и по делу: CTA — глагол + объект («Забронировать помещение», «Зарегистрироваться на мероприятие»).

**Чего избегать в интерфейсе:** официально-бюрократических формулировок (их место — только в обязательных юридических ссылках футера), длинных предложений, «выканья».

---

## 20. Сводка CSS-токенов

> Значения приблизительные (см. оговорку в начале). Используйте как стартовый набор переменных.

```css
:root {
  /* --- Цвет: нейтральные --- */
  --bg: #000000;
  --surface: #151515;
  --surface-elevated: #1F1F1F;
  --stroke: rgba(255, 255, 255, .08);
  --text-primary: #FFFFFF;
  --text-secondary: #A2A2A2;
  --text-muted: #6B6B6B;

  /* --- Цвет: акценты --- */
  --violet: #8C52FF;
  --violet-deep: #5B2BD6;
  --lime: #C8FF1E;

  /* --- Цвет: светлые поверхности --- */
  --white-surface: #FFFFFF;
  --cream: #F1EFEA;
  --ink-on-light: #141414;

  /* --- Затемнение модалки --- */
  --backdrop: rgba(0, 0, 0, .6);

  /* --- Типографика --- */
  --font-display: "Unbounded", "Involve", "TT Norms Pro", system-ui, sans-serif;
  --font-text: "Onest", "Inter", "Manrope", system-ui, sans-serif;

  --fs-hero: clamp(72px, 14vw, 180px);
  --fs-section: clamp(48px, 6vw, 80px);
  --fs-modal-title: 44px;
  --fs-card-title: 30px;
  --fs-nav: 18px;
  --fs-button: 18px;
  --fs-body: 17px;
  --fs-label: 14px;

  --lh-tight: 1.05;
  --lh-body: 1.45;

  /* --- Скругления --- */
  --radius-pill: 999px;
  --radius-sm: 16px;
  --radius-card: 24px;
  --radius-nav: 28px;
  --radius-section: 32px;

  /* --- Отступы (4px-база) --- */
  --space-1: 4px;   --space-2: 8px;   --space-3: 12px;  --space-4: 16px;
  --space-5: 24px;  --space-6: 32px;  --space-7: 48px;  --space-8: 64px;
  --space-9: 96px;  --space-10: 128px;

  /* --- Контейнер --- */
  --container-max: 1440px;
  --container-pad: 40px;
}
```

---

### Как сделать значения точными

Этот гайд снят визуально. Чтобы получить **точные** цвета, шрифты и размеры:

1. Открыть сайт → DevTools (`F12`) → выбрать элемент → вкладка **Computed** (готовые `color`, `font-family`, `font-size`, `border-radius`).
2. Либо дать мне доступ к доменам `prostospb.team` и `tildacdn.com` в настройках сети — тогда я вытащу реальные значения из CSS и обновлю токены побайтово.

*Документ составлен на основе главной страницы и экранов записи/входа `prostospb.team` (по состоянию на май 2026).*
